// code that improves performance with fewer bugs
// static analyser to find problems of code (something like grammar checker)
// compiler cannot find all mistakes and analyze what user have written
// static analyzer can detect various unintended errors e.g. PVSStudio
#include <iostream>
#include <string>
#define LOG(x) std::cout << x << std::endl

int main() {

	std::cin.get();
}